

    <dl>
      <% @person.each do |key, value| %>
      <%= 
        # get the string equivalent of the symbol
        key_str = key.id2name 
        if key_str["_"]
          # replce _ with a space
          key_str["_"] = " "
        end
      %>
      
      <dt><%= "#{key_str}" %></dt>
      <dd> &nbsp; <%= " #{value}" %></dd>
      <% end %>
    </dl>
  